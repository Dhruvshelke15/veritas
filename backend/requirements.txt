fastapi==0.139.0
uvicorn==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
langchain-core==1.4.9
langchain-text-splitters==1.1.2
pypdf==6.14.2
chromadb==1.5.9
# Not used directly for embeddings anymore (see app/ingestion/indexer.py —
# embeddings are hosted via HuggingFace's Inference API to avoid PyTorch's
# memory footprint in the deployed process). Still required as an installed
# package because langchain_text_splitters' __init__.py unconditionally
# imports SentenceTransformersTokenTextSplitter, even though this project
# only uses the plain RecursiveCharacterTextSplitter.
sentence-transformers==5.6.0
# Direct dependency, not just transitive via sentence-transformers: used
# explicitly in app/ingestion/embeddings.py to call HF's current
# router.huggingface.co-based Inference Providers routing (chromadb's own
# HuggingFaceEmbeddingFunction posts to the decommissioned
# api-inference.huggingface.co domain instead).
huggingface_hub==1.24.0
pytest==9.1.1
httpx==0.28.1
anthropic==0.116.0
# TensorFlow has no wheel for Python 3.14 (any platform) and no tensorflow-cpu
# wheel for macOS at all. Install into a Python 3.12 venv: see TESTING.md.
tensorflow-cpu==2.21.0; sys_platform != "darwin"
tensorflow==2.21.0; sys_platform == "darwin"
python-dotenv==1.2.2
ruff==0.15.22
beautifulsoup4==4.15.0
